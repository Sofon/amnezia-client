name: 'Release deploy workflow'

# Disabled - only Windows builds are needed
# on:
#   workflow_dispatch:

on:
  workflow_dispatch:
    inputs:
      placeholder:
        description: 'Disabled - Android builds removed'
        required: false

jobs:
  placeholder:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled"
